runtime: python39 

entrypoint: gunicorn -b :$PORT app:app  

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto

env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: "C:/Users/temir/cap5930-project-1-da121717363a.json" # edit the path for the credentials